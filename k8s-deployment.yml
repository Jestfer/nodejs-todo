apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-todo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodejs-todo
  # strategy:
  #   rollingUpdate:
  #     maxSurge: 1
  #     maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: nodejs-todo
    spec:
      containers:
      - name: nodejs-todo
        image: jestfer/nodejs-todo
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
          limits:
            cpu: 100m
---
apiVersion: v1
kind: Service
metadata:
  name: nodejs-todo-service
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: nodejs-todo
